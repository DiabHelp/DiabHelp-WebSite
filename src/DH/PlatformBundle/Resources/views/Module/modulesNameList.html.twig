    <ul class="nav_second" >
        {% if modules %}
            {% for module in modules %}
                {% if module.display == 1 %}
                    <li>
                        <a href='{{ path('dh_platform_module_view', {'id': module.id}) }}'>{{ module.name | truncate(20) }}</a>
                    </li>
                {% endif %}
            {% endfor %}
        {% else %}
            <li>
                {{ 'Pas de module trouvé' | trans }}
            </li>
        {% endif %}
    </ul>
